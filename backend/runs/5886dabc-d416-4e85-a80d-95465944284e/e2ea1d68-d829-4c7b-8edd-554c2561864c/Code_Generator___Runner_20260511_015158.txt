Pipeline: Code Generator & Runner
Execution Time: 2026-05-11T01:51:58.672513Z
==================================================

✅ [n1 | inputText] Executed successfully
✅ [n2 | coder] Executed successfully
✅ [n3 | execution] Executed successfully
   -> WARNING/ERROR: Code execution node not natively supported in headless yet. Passing through code.
✅ [n4 | outputText] Executed successfully

==================================================
FINAL PIPELINE OUTPUT(S):
--- OUTPUT FROM n4 ---
Here is a Python script that calculates the Fibonacci sequence:
```
def fibonacci(n):
    """
    Calculate the nth Fibonacci number.

    Args:
        n (int): The position of the Fibonacci number to calculate (0-based).

    Returns:
        int: The nth Fibonacci number.
    """
    if n <= 1:
        return n

    a, b = 0, 1
    for i in range(2, n + 1):
        a, b = b, a + b

    return b


# Test the function
print(fibonacci(10))  # Output: 55
```
Here's an explanation of the code:

* The `fibonacci` function takes an integer `n` as input and returns the `n`-th Fibonacci number.
* If `n` is 0 or 1, we return `n` directly, since these are the base cases for the Fibonacci sequence.
* For `n > 1`, we use a loop to calculate the `n`-th Fibonacci number. We initialize two variables `a` and `b` to the first two Fibonacci numbers (0 and 1). Then, in each iteration of the loop, we update `a` and `b` to the next two Fibonacci numbers using the recurrence relation `a, b = b, a + b`.
* Finally, we return the last calculated value of `b`, which is the `n`-th Fibonacci number.

The test call at the bottom uses the exact function name `fibonacci` and variable names defined in the code, as requested.