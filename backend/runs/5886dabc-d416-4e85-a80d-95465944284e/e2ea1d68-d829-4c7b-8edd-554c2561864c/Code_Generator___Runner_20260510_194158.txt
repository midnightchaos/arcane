Pipeline: Code Generator & Runner
Execution Time: 2026-05-10T19:41:58.352427Z
==================================================

✅ [n1 | inputText] Executed successfully
✅ [n2 | coder] Executed successfully
✅ [n3 | execution] Executed successfully
   -> WARNING/ERROR: Code execution node not natively supported in headless yet. Passing through code.
✅ [n4 | outputText] Executed successfully

==================================================
FINAL PIPELINE OUTPUT(S):
--- OUTPUT FROM n4 ---
Here is a Python script that calculates the Fibonacci sequence:
```python
def fibonacci(n):
    """
    Calculate the nth Fibonacci number using recursion and memoization.
    
    Args:
        n (int): The position of the Fibonacci number to calculate.
        
    Returns:
        int: The nth Fibonacci number.
    """
    memo = {0: 0, 1: 1}
    
    def fib_helper(k):
        if k not in memo:
            memo[k] = fib_helper(k-1) + fib_helper(k-2)
        return memo[k]
    
    return fib_helper(n)

# Test call
print(fibonacci(10))  # Should print the 10th Fibonacci number
```
This script uses a recursive function `fib_helper` to calculate the Fibonacci sequence, with memoization to optimize performance. The `fibonacci` function takes an integer `n` as input and returns the `n`-th Fibonacci number.

The test call at the bottom uses the exact function and variable names defined in the code, as required. You can modify the value passed to `fibonacci` (e.g., `5` or `20`) to calculate a different position in the sequence.

Note: This script assumes that the input `n` is a non-negative integer. If you want to handle negative integers or non-integer inputs, you'll need to add additional error handling and validation.