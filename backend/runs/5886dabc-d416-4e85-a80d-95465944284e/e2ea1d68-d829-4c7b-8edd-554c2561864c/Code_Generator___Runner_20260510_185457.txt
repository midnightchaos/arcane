Pipeline: Code Generator & Runner
Execution Time: 2026-05-10T18:54:57.603684Z
==================================================

✅ [n1 | inputText] Executed successfully
✅ [n2 | coder] Executed successfully
✅ [n3 | execution] Executed successfully
   -> WARNING/ERROR: Code execution node not natively supported in headless yet. Passing through code.
✅ [n4 | outputText] Executed successfully

==================================================
FINAL PIPELINE OUTPUT(S):
--- OUTPUT FROM n4 ---
Here is a Python script that calculates the Fibonacci sequence:
```
def fibonacci(n):
    """
    Calculate the nth Fibonacci number.

    :param n: The position of the Fibonacci number to calculate (1-indexed).
    :return: The nth Fibonacci number.
    """
    if n <= 0:
        return None

    if n == 1:
        return 0
    elif n == 2:
        return 1

    a, b = 0, 1
    for i in range(2, n):
        a, b = b, a + b

    return b

# Test the function
print(fibonacci(10))  # Should print 55
```
Here's how it works:

* The `fibonacci` function takes an integer `n` as input and returns the `n`-th Fibonacci number.
* We handle the base cases for `n` <= 2 separately, since these values are trivially 0 or 1.
* For larger values of `n`, we use a loop to calculate the Fibonacci sequence iteratively. We keep track of the previous two numbers in the sequence (`a` and `b`) and update them at each step using the recurrence relation `a, b = b, a + b`.
* Finally, we return the `n`-th Fibonacci number, which is stored in the variable `b`.

The test call at the bottom uses the exact function name `fibonacci` and calculates the 10th Fibonacci number.