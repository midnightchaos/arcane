Pipeline: Code Generator & Runner
Execution Time: 2026-05-10T19:32:28.249079Z
==================================================

✅ [n1 | inputText] Executed successfully
✅ [n2 | coder] Executed successfully
✅ [n3 | execution] Executed successfully
   -> WARNING/ERROR: Code execution node not natively supported in headless yet. Passing through code.
✅ [n4 | outputText] Executed successfully

==================================================
FINAL PIPELINE OUTPUT(S):
--- OUTPUT FROM n4 ---
Here is a Python script that calculates the Fibonacci sequence:
```python
def fibonacci(n):
    """
    Calculate the nth Fibonacci number.

    Args:
        n (int): The position of the Fibonacci number to calculate.

    Returns:
        int: The Fibonacci number at position n.
    """
    if n <= 1:
        return n

    a, b = 0, 1
    for i in range(2, n + 1):
        a, b = b, a + b

    return b


# Test call
print(fibonacci(10))  # Output: 55
```
Here's a breakdown of the code:

* The `fibonacci` function takes an integer `n` as input and returns the `n`-th Fibonacci number.
* The function uses recursion to calculate the first two Fibonacci numbers (0 and 1), then iterates from 2 to `n` using a loop.
* In each iteration, it updates the previous two Fibonacci numbers (`a` and `b`) by adding them together and storing the result in `b`.
* Finally, it returns the `n`-th Fibonacci number (`b`).
* The test call at the bottom prints the 10th Fibonacci number using the `fibonacci` function.

Note that this implementation uses a iterative approach to calculate the Fibonacci sequence, which is generally more efficient than recursive approaches for large values of `n`.