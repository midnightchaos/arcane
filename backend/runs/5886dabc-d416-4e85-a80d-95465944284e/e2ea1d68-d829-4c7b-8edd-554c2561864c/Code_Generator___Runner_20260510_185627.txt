Pipeline: Code Generator & Runner
Execution Time: 2026-05-10T18:56:27.591789Z
==================================================

✅ [n1 | inputText] Executed successfully
✅ [n2 | coder] Executed successfully
✅ [n3 | execution] Executed successfully
   -> WARNING/ERROR: Code execution node not natively supported in headless yet. Passing through code.
✅ [n4 | outputText] Executed successfully

==================================================
FINAL PIPELINE OUTPUT(S):
--- OUTPUT FROM n4 ---
Here is a Python script that calculates the Fibonacci sequence:
```
def fibonacci(n):
    """
    Calculate the nth Fibonacci number.

    Args:
        n (int): The position of the Fibonacci number to calculate.

    Returns:
        int: The nth Fibonacci number.
    """
    if n <= 1:
        return n

    a, b = 0, 1
    for _ in range(2, n + 1):
        a, b = b, a + b

    return b


# Test the function
print(fibonacci(10))  # Output: 55
```
Let me explain what's going on:

* The `fibonacci` function takes an integer `n` as input and returns the `n`-th Fibonacci number.
* If `n` is 0 or 1, we return `n` directly, since the first two Fibonacci numbers are 0 and 1.
* Otherwise, we use a loop to calculate the `n`-th Fibonacci number. We keep track of the previous two numbers in variables `a` and `b`, and update them using the recursive formula: `a` becomes `b`, and `b` becomes `a + b`.
* Finally, we return the value of `b`, which is the `n`-th Fibonacci number.

The test call at the bottom simply prints the 10th Fibonacci number, which should output `55`.

Note that this implementation has a time complexity of O(n), making it efficient for large values of `n`.