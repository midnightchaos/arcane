Pipeline: Code Generator & Runner
Execution Time: 2026-05-10T18:49:27.515780Z
==================================================

✅ [n1 | inputText] Executed successfully
✅ [n2 | coder] Executed successfully
✅ [n3 | execution] Executed successfully
   -> WARNING/ERROR: Code execution node not natively supported in headless yet. Passing through code.
✅ [n4 | outputText] Executed successfully

==================================================
FINAL PIPELINE OUTPUT(S):
--- OUTPUT FROM n4 ---
Here is a Python script that calculates the Fibonacci sequence:
```
def fibonacci(n):
    """
    Calculate the nth Fibonacci number.

    Args:
        n (int): The position of the Fibonacci number to calculate (0-based).

    Returns:
        int: The nth Fibonacci number.
    """
    if n <= 1:
        return n
    a, b = 0, 1
    for _ in range(2, n + 1):
        a, b = b, a + b
    return b

# Test call
print(fibonacci(10))  # Should print the 10th Fibonacci number
```
This script defines a recursive function `fibonacci` that calculates the nth Fibonacci number. The test call at the bottom uses the exact function name and argument value to verify the correctness of the code.

Note: This implementation is not optimized for large values of `n`, as it has a time complexity of O(n). For larger values, you may want to consider using an iterative approach or memoization techniques to improve performance.