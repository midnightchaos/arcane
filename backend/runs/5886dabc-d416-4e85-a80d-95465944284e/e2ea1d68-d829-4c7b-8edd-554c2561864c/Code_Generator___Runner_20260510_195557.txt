Pipeline: Code Generator & Runner
Execution Time: 2026-05-10T19:55:57.752290Z
==================================================

✅ [n1 | inputText] Executed successfully
✅ [n2 | coder] Executed successfully
✅ [n3 | execution] Executed successfully
   -> WARNING/ERROR: Code execution node not natively supported in headless yet. Passing through code.
✅ [n4 | outputText] Executed successfully

==================================================
FINAL PIPELINE OUTPUT(S):
--- OUTPUT FROM n4 ---
Here is a Python script that calculates the Fibonacci sequence:
```
def fibonacci(n):
    """
    Calculate the nth Fibonacci number.

    Args:
        n (int): The index of the Fibonacci number to calculate.

    Returns:
        int: The nth Fibonacci number.
    """
    if n <= 1:
        return n
    a, b = 0, 1
    for i in range(2, n+1):
        a, b = b, a + b
    return b

# Test call
print(fibonacci(10))  # Should print the 10th Fibonacci number
```
This script uses a simple iterative approach to calculate the Fibonacci sequence. The `fibonacci` function takes an integer `n` as input and returns the nth Fibonacci number.

The test call at the bottom calls the `fibonacci` function with `n=10`, which should print the 10th Fibonacci number (which is 55).

Note that I've included docstrings to provide documentation for the `fibonacci` function, including its arguments and return value. This follows best practices for writing readable and maintainable code.

Also, I've double-checked that the test call uses the exact function and variable names defined in the code, as per your request!