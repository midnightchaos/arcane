Pipeline: Code Generator & Runner
Execution Time: 2026-05-10T18:39:57.373061Z
==================================================

✅ [n1 | inputText] Executed successfully
✅ [n2 | coder] Executed successfully
✅ [n3 | execution] Executed successfully
   -> WARNING/ERROR: Code execution node not natively supported in headless yet. Passing through code.
✅ [n4 | outputText] Executed successfully

==================================================
FINAL PIPELINE OUTPUT(S):
--- OUTPUT FROM n4 ---
Here is a Python script that calculates the Fibonacci sequence:
```
def fibonacci(n):
    """
    Calculate the nth Fibonacci number.

    Args:
        n (int): The position of the Fibonacci number to calculate.

    Returns:
        int: The nth Fibonacci number.
    """
    if n <= 1:
        return n
    a, b = 0, 1
    for _ in range(2, n+1):
        a, b = b, a + b
    return b

# Test call
print(fibonacci(10))  # Should print: 55
```
Let me explain the code:

* The `fibonacci` function takes an integer `n` as input and returns the `n`-th Fibonacci number.
* If `n` is 0 or 1, we simply return `n`, since the first two Fibonacci numbers are 0 and 1.
* Otherwise, we use a loop to calculate the `n`-th Fibonacci number. We keep track of the previous two numbers in the sequence using variables `a` and `b`.
* In each iteration, we update `a` and `b` by shifting them one position forward in the sequence. This is done by setting `a` to `b` and `b` to the sum of the previous values.
* Finally, we return the calculated `n`-th Fibonacci number.

The test call at the bottom uses the exact function name and variable names defined in the code, as required.