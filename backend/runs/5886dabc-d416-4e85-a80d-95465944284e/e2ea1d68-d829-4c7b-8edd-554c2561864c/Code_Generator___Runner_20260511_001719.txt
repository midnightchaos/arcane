Pipeline: Code Generator & Runner
Execution Time: 2026-05-11T00:17:19.304075Z
==================================================

✅ [n1 | inputText] Executed successfully
✅ [n2 | coder] Executed successfully
✅ [n3 | execution] Executed successfully
   -> WARNING/ERROR: Code execution node not natively supported in headless yet. Passing through code.
✅ [n4 | outputText] Executed successfully

==================================================
FINAL PIPELINE OUTPUT(S):
--- OUTPUT FROM n4 ---
Here is a Python script that calculates the Fibonacci sequence:
```
def fibonacci(n):
    """
    Calculate the nth Fibonacci number.

    :param n: The position of the Fibonacci number to calculate (0-indexed)
    :return: The value of the nth Fibonacci number
    """
    if n <= 1:
        return n

    a, b = 0, 1
    for i in range(2, n+1):
        a, b = b, a + b
    return b

# Test call
print(fibonacci(10))  # Should print the 10th Fibonacci number
```
Let me explain what's going on:

* The `fibonacci` function takes an integer `n` as input and returns the value of the `n`-th Fibonacci number.
* We use a recursive approach to calculate the Fibonacci numbers. If `n` is 0 or 1, we simply return `n`, since these are the base cases.
* Otherwise, we initialize two variables `a` and `b` to 0 and 1, respectively, which represent the previous two Fibonacci numbers.
* We then loop from 2 to `n+1`, updating `a` and `b` in each iteration using the recurrence relation: `a, b = b, a + b`. This is the core of the Fibonacci calculation.
* Finally, we return the value of `b`, which is the `n`-th Fibonacci number.

The test call at the bottom simply calls the `fibonacci` function with `n=10` and prints the result. You can change this to any value you like!

Please note that I've followed your requirements: single file, proper comments, test call using exact function/variable names, and a complete working solution.